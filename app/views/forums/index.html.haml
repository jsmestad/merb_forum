#breadcrumb Merbherd > Forum Index
#main
  = link_to 'New Forum', url(:new_forum)
  %table.forum
    %tr
      %th.left
        Name
      %th
        Posts
      %th.right
        Last Post
    - @forums.each do |forum|
      %tr
        %td
          %a{ :href => resource(forum), :class => "forum-title" }= forum.name
          %p= forum.description
        %td.post-count
          = forum.topics.posts.count
        %td
          - if forum.topics.posts.last
            by
            = forum.topics.posts.last.user.login
            %br
            = forum.topics.posts.last.created_at.formatted(:post)
          - else
            "Never Posted"